function init() {
    if(debug === true) {
        console.log("init is active");
    }
    generateCanvas(400,400);
    background("green");
}

function loop() {
    if(debug === true) {
        console.log("loop function active");
    }
}